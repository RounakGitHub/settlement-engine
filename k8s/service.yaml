apiVersion: v1
kind: Service
metadata:
  name: settlement-engine-api
  labels:
    app: settlement-engine
    component: api
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
  selector:
    app: settlement-engine
    component: api
